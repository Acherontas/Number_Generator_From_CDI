#include "generator_feeder.h"
#include <iomanip>
#include "math.h"
#include "sum_cdi.h"
using namespace std;
//ctor
generator_feeder::generator_feeder(){}
//dtor
generator_feeder::~generator_feeder(){}
//copy ctor
generator_feeder::generator_feeder(const generator_feeder& other){}
//handle self assignment//assignment operator
generator_feeder& generator_feeder::operator=(const generator_feeder& rhs){if (this == &rhs) return *this; return *this;}
int generator_feeder::gener_feed(){
        sum_cdi *scd=new sum_cdi();
        long double xi;
        xend=0;
        cout<<"for cdi of " << f_cdi <<" \n";
        
        
        
    for(int i=0;i<10;i++){
        xi=0;
         nn=pow(10,lt_of_each[i]);
         startt=nn*ch_el[i];
         endd=startt+(nn-1);
         
        for(int i=startt;i<=endd;i++){
            xi=scd->cdis(i,3);
            if(xi==f_cdi){
                    //cout<< i <<" ";
                   
                    //if(xend%18==0){cout<<"\n";}
                   // xend+=1;
            }
        }
    }
    cout<<"\n";
    return 0;
}
int generator_feeder::show_them(){
    for(int i=0;i<=10;i++){
            cout<<fnumber[rndd];
            }
    for(int i=0;i<=10;i++){
            cout<<fnumber_t[rndd];
            }
   /* for(int i=0;i<=10;i++){
            if(ch_le[i]<=999){rndd =rand()%999;cout<<fnumber_th[rndd];}
            }
    for(int i=0;i<=10;i++){
            if(ch_le[i]<=9999){rndd =rand() %9999;cout<<fnumber_f[rndd];}
            }
    for(int i=0;i<=10;i++){
            if(ch_le[i]<=99999){rndd =rand() %99999;cout<<fnumber_s[rndd];}
            }
    for(int i=0;i<=10;i++){
            if(ch_le[i]<=999999){rndd =rand() %999999;cout<<fnumber_sv[rndd];}
            }
    for(int i=0;i<=10;i++){
            if(ch_le[i]<=9999999){rndd =rand() %9999999;cout<<fnumber_ei[rndd];}
            }
    for(int i=0;i<=10;i++){
            if(ch_le[i]<=99999999){rndd =rand() %99999999;cout<<fnumber_ni[rndd];}
            } */
       return 0;
    }
